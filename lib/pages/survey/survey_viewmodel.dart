import 'package:flutter/material.dart';
import 'package:odak/survey/survey.dart';

abstract class SurveyViewModel extends State<Survey> {

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
